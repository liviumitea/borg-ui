apiVersion: v1
kind: Namespace
metadata:
  name: borg-ui
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: borg-data
  namespace: borg-ui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: borg-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          imagePullPolicy: IfNotPresent
          command:
            - redis-server
            - --maxmemory
            - 2gb
            - --maxmemory-policy
            - allkeys-lru
            - --save
            - ""
            - --appendonly
            - "no"
          ports:
            - containerPort: 6379
              name: redis
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: borg-ui
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: borg-web-ui
  namespace: borg-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: borg-web-ui
  template:
    metadata:
      labels:
        app: borg-web-ui
    spec:
      securityContext:
        fsGroup: 1001
        fsGroupChangePolicy: "OnRootMismatch"
      initContainers:
        - name: init-permissions
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /data
              chown -R 1001:1001 /data
              chmod -R ug+rwX /data
              ls -ld /data
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            - name: borg-data
              mountPath: /data
      containers:
        - name: borg-web-ui
          image: ainullcode/borg-ui:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            runAsUser: 0
            runAsGroup: 0
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: PORT
              value: "8081"
            - name: ENVIRONMENT
              value: "production"
            - name: PUID
              value: "1001"
            - name: PGID
              value: "1001"
            - name: TZ
              value: "Europe/Bucharest"
            - name: LOCAL_MOUNT_POINTS
              value: "/backups"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_DB
              value: "0"
            - name: CACHE_TTL_SECONDS
              value: "7200"
            - name: CACHE_MAX_SIZE_MB
              value: "2048"
            - name: ACTIVATION_SERVICE_URL
              value: "https://license.borgui.com"
            - name: ACTIVATION_TIMEOUT_SECONDS
              value: "10"
            - name: ENABLE_STARTUP_LICENSE_SYNC
              value: "true"
          livenessProbe:
            httpGet:
              path: /
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 10
          volumeMounts:
            - name: borg-data
              mountPath: /data
            - name: borg-cache
              mountPath: /home/borg/.cache/borg
            - name: host-localtime
              mountPath: /etc/localtime
              readOnly: true
            - name: backup-host-path
              mountPath: /backups
      volumes:
        - name: borg-data
          persistentVolumeClaim:
            claimName: borg-data
        - name: borg-cache
          emptyDir: {}
        - name: host-localtime
          hostPath:
            path: /etc/localtime
            type: File
        - name: backup-host-path
          hostPath:
            path: /data-nfs/data/borg
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: borg-web-ui
  namespace: borg-ui
spec:
  selector:
    app: borg-web-ui
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: LoadBalancer
  loadBalancerIP: 172.16.23.40
